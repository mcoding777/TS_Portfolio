{
    "compilerOptions": {
        "target": "es5", // 뭘로 변환할건지? 'es3', 'es5', 'es2015', 'es2016', 'es2017','es2018', 'esnext' 가능
        "allowJs": true, // js 파일들 ts에서 import해서 쓸 수 있는지 
        "skipLibCheck": true, // 라이브러리 확인 건너뛰기로 정확성은 낮추지만 컴파일 시간 절약함
        "esModuleInterop": true, // ES 모듈 상호작용
        "allowSyntheticDefaultImports": true, // import * as React from "react"; 같은 가져오기 작성 가능
        "strict": true, // strict 관련, noimplicit 어쩌구 관련 모드 전부 켜기
        "strictNullChecks": true, // null, undefined 타입에 이상한 짓 할시 에러내기 
        "forceConsistentCasingInFileNames": true,
        "noFallthroughCasesInSwitch": true, // switch문 이상하면 에러내기 
        "module": "esnext", // 무슨 import 문법 쓸건지 'commonjs', 'amd', 'es2015', 'esnext'
        "jsx": "react", // tsx 파일을 jsx로 어떻게 컴파일할 것인지 'preserve', 'react-native', 'react'
        "noUnusedLocals": false, //쓰지않는 지역변수 있으면 에러내기
        "noUnusedParameters": true, //쓰지않는 파라미터 있으면 에러내기
        "moduleResolution": "node",
    },
    "include": ["src"]
}
